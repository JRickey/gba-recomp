# Test data — local ROM corpus, maps, and work dirs, never committed.
# (The shipped gamedb.sqlite lives at the repo root, not here.)
/data

# Local working notes — never committed
/CLAUDE.md
/notes

# Build artifacts
/target
*.o
*.so
*.dylib
*.dll

# Recompiler output
/out

# Editor/OS noise
.DS_Store
*.swp
.idea/
.vscode/
